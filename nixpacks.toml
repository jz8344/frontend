[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node server.js'

[staticAssets]
# Deshabilitar Caddy - usaremos nuestro propio servidor Express
enable = false
